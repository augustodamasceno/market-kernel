## Core Rules
> Before performing any task, read and follow the files listed below at the repository root.
> Those files are the single source of truth for coding standards, conventions, and project-specific rules.
> Do not replicate their content in this file or any other LLM instruction file.
> Only apply rules from **`project-instructions.md`** if the `<PROJECT_NAME>` placeholder has been replaced with an actual project name. If it still contains `<PROJECT_NAME>`, treat the file as an unfilled template and ignore it.

### Files to read
- `instructions.md`
- `project-instructions.md`

### Language-specific files to read
*(Keep only the languages used in this project. See README for the full list of available files.)*
- `instructions-language-cpp.md`
